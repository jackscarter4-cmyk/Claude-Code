# Core (needed to run the simulation, tracking, calibration, safety, tests)
numpy>=1.24
pytest>=7.0

# Hardware-only (install on the Jetson/Raspberry Pi; imported lazily so the
# package still runs on a dev machine without them):
#   spidev            # MCP4922 DAC over SPI (galvo aiming)
#   RPi.GPIO          # laser enable line
#   opencv-python     # camera capture + YOLO stereo detection
#   onnxruntime-gpu   # or ultralytics, for the detector model
