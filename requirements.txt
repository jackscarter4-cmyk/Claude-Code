# Offline Stock Quant Engine
# Python 3.11+
#
# This build runs entirely offline and uses ONLY the Python standard library.
# There are no third-party dependencies and no API keys to install.
#
# Used (all stdlib):
#   argparse, csv, json, math, sqlite3, dataclasses,
#   datetime, pathlib, typing, logging
#
# Just run:
#   python main.py --template > stocks.csv
#   python main.py --input stocks.csv
